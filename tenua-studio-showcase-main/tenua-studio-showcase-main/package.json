{
  "name": "tenua-studio-forwarder",
  "private": true,
  "version": "0.0.0",
  "description": "Forward build/dev commands to parent app to satisfy Vercel prefix path",
  "scripts": {
    "dev": "npm run dev --prefix ..",
    "build": "npm run build --prefix ..",
    "preview": "npm run preview --prefix ..",
    "lint": "npm run lint --prefix .."
  }
}

